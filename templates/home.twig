{% extends 'base.twig' %}

{% block title %}Home{% endblock %}

{% block content %}
    <section>
        <div class="container">
            <div class="entry-list">
                {% for post in posts %}
                <article>
                    <h2><a href="/{{ post.title|lower|replace({' ':'-'}) }}">{{ post.title|title }}</a></h2>
                    <time datetime="{{ post.date }}">{{ post.date|date('F d, Y \\a\\t  g:i a') }}</time>
                </article>
                {% endfor %}
            </div>
        </div>
    </section>
{% endblock %}